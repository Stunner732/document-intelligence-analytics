# Application
APP_NAME=document-intelligence-analytics
APP_ENV=development
LOG_LEVEL=INFO

# PostgreSQL (local development defaults only; replace through an untracked .env file)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=document_intelligence
POSTGRES_USER=document_app
POSTGRES_PASSWORD=change_me

# API
API_HOST=0.0.0.0
API_PORT=8000
